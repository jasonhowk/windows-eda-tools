# Scripts

This directory will contain Windows packaging and convenience scripts, including:

- Application build and packaging helpers
- Manifest and checksum validation
- A combined `install-all.ps1` helper after the individual packages are working
